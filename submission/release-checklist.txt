Release checklist for Karan/Rowan (developer cannot push per ticket policy).

## Quick start (Karan)

1. From this repo: `npm run test`
2. `sh submission/release-preflight.sh` (requires gh auth as karanp0202)
3. `sh submission/update-github-pr-description.sh` (or paste submission/pr-4768-body.md on GitHub)
4. Complete submission/identity-decision.txt (check one option A/B/C)
5. If preflight reports head mismatch: `git push karanp0202 HEAD:fix/4764-candidate-fix-for-archived-autorouti`

Note: older coordination notes citing PR remote `2fac6c4` are obsolete — always compare `git rev-parse --short HEAD` to live PR #4768 via preflight.

## 1. PR branch status

Compare local HEAD to PR #4768 branch on GitHub (run `git rev-parse --short HEAD` locally).

If they differ:
  git push karanp0202 HEAD:fix/4764-candidate-fix-for-archived-autorouti

**Still needed on GitHub:** PR #4768 description still shows iteration-7 text (no scope/posting-coordination sections). Branch code should match local HEAD when last pushed.

Option A — full preflight (local checks + live PR status via gh):
  sh submission/release-preflight.sh

Option B — update description only (requires gh auth as karanp0202):
  sh submission/update-github-pr-description.sh
  sh submission/update-github-pr-description.sh --dry-run   # preview only

Option C — on GitHub, click Edit on PR #4768 and paste submission/pr-4768-body.md.

## 2. Posting identity — BLOCKING, decide before merge or issue comment

| Account | Role |
| --- | --- |
| AMZ92 | Opened issue #4764; posted original narrow candidate; validated narrow vs superset on 2026-09-07 |
| karanp0202 | Opened PR #4768 (submission packet on tscircuit) |
| 85heisenberg85 | Posted complementary superset patch; explicitly deferred bounty priority to AMZ92 |

Rowan/Karan must confirm one of:
  A) AMZ92 and karanp0202 are the same contributor → proceed with PR as AMZ92's packet
  B) Different contributors → close or coordinate; do not merge duplicate narrow patch
  C) Compose with 85heisenberg85/autorouting@fix-issue92-wild-jumps (superset includes our exact parent-obstacleHit removal)

Record the decision in submission/identity-decision.txt before merge or issue comment.

See submission/thread-context.txt for Sep 2026 issue-thread summary.

Do not invent policy. Do not post competing claims without reconciliation.

## 3. Autorouting fork PR (narrow fix only)

If maintainers direct a fork PR for the archived repo:
  Branch: fix/forward-after-obstacle @ 2a3eb3b
  Files: see submission/upstream-scope.txt (3 files + patch at 02dcdb6)
  Patch: plain unified diff at submission/autorouting-92-candidate.patch
  Do NOT include: package.json test override, verify script, submission/*, .agent-nio/*

## 4. Issue comment

AMZ92 already posted validation evidence on #4764 — do not duplicate.
Post submission/issue-4764-reply.txt only if identity confirmed and additional maintainer questions remain unanswered.

## 5. Algora workflow

Autorouting repo is archived. /attempt and /claim deferred until maintainers confirm submission route.
